-> Fix loading states for: random. 
-> Fix buttons and button component

-> Only show info that actually exists, if not, show in a box that tells which info isnt available (put one on top of movie and one on top of prod detail)
    -> When showing lists, only show if have poster.
    -> When showingn movie, show all info and separate those that dont exist in a box

-> Responsividade -> movie/:id

TODO: 
    -> Games
    -> Add ordering to filter (by date, popularity etc) 
    -> Dark mode
    -> Add tutorial or message boxes to show how to use each functionality